{
  "name": "saturasa-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create saturasa-db",
    "db:migrate": "wrangler d1 migrations apply saturasa-db"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.0",
    "wrangler": "~3.114.0"
  }
}

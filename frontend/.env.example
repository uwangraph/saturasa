# Alamat Backend Cloudflare Workers
# Ganti dengan URL Workers-mu setelah dideploy (tanpa tanda miring di akhir)
# Contoh: VITE_API_BASE=https://saturasa.username.workers.dev
VITE_API_BASE=http://localhost:8787
